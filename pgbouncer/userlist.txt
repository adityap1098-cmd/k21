# PgBouncer user list
# Format: "username" "md5<md5hash_of_password_plus_username>"
# Generate hash: echo -n "${POSTGRES_PASSWORD}${POSTGRES_USER}" | md5sum | awk '{print "md5"$1}'
#
# ⚠️  C-08: The hash below MUST be replaced before production deploy.
#     Use the entrypoint script or generate manually:
#     echo -n "YOUR_PASSWORDappuser" | md5sum | awk '{print "md5"$1}'
#     Then replace md5CHANGEME with the output.
"appuser" "md5CHANGEME"
