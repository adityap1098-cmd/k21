---
phase: 06-marketplace-integration
plan: 10
type: verify
wave: 7
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06, 06-07, 06-08, 06-09]
autonomous: false
requirements:
  - MKT-01
  - MKT-02
  - MKT-03
  - MKT-04
  - MKT-05
  - MKT-06
  - MKT-07
  - MKT-08

must_haves:
  truths:
    - "Full test suite passes: all marketplace tests GREEN"
    - "TypeScript compiles with no errors"
    - "All 8 MKT requirements are covered by tests"
    - "Human checkpoint: review webhook handler, token refresh, order lifecycle"
---

# Plan 06-10: Full Suite Verification + Human Checkpoint

**Wave 7 — Final Verification**

## Goal

Run full test suite, verify all MKT requirements are covered, and get human sign-off.

## Verification Checklist

### Automated
- [ ] `pnpm --filter @k21/api exec vitest run` — all tests pass
- [ ] `pnpm --filter @k21/api exec tsc --noEmit` — no TypeScript errors
- [ ] `pnpm --filter @k21/web exec tsc --noEmit` — no TypeScript errors

### MKT Requirement Coverage
- [ ] **MKT-01:** Shopee order import via webhook → marketplace_order created
- [ ] **MKT-02:** TikTok order import via webhook → marketplace_order created
- [ ] **MKT-03:** Stock changes push to Shopee + TikTok via BullMQ
- [ ] **MKT-04:** Order lifecycle: confirm→reserve, cancel→release, ship→movement+journal
- [ ] **MKT-05:** All marketplace processing runs as BullMQ background jobs
- [ ] **MKT-06:** Webhook handler validates HMAC, stores raw, enqueues, returns 200 < 100ms
- [ ] **MKT-07:** Duplicate webhook events (same platform+event_type+event_id) discarded
- [ ] **MKT-08:** Token refresh runs automatically before expiry, alerts on consecutive failure

### Human Review
- [ ] Webhook handler code review: is it truly thin? No business logic?
- [ ] Token storage: secrets encrypted at rest? (or at minimum not logged)
- [ ] Order lifecycle: edge cases covered? (partial ship, partial cancel)
- [ ] Stock sync debounce: is 5-second window appropriate?
- [ ] UI: channels page, orders tab, SKU mapping usable?
