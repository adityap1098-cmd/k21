---
phase: 01-auth-rbac
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/modules/auth/auth.test.ts
  - apps/api/src/modules/users/users.test.ts
  - apps/api/src/middleware/authenticate.test.ts
  - apps/api/src/middleware/require-role.test.ts
  - apps/api/src/db/schema/users.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-08

must_haves:
  truths:
    - "Running `pnpm --filter @k21/api test` completes without config errors"
    - "All 5 test stubs are in RED state (tests fail because implementation does not exist yet)"
    - "jose, argon2, cookie-parser packages are installed in @k21/api"
    - "zod is installed in @k21/api"
  artifacts:
    - path: "apps/api/src/modules/auth/auth.test.ts"
      provides: "Test stubs for AUTH-01, AUTH-02, AUTH-03"
    - path: "apps/api/src/modules/users/users.test.ts"
      provides: "Test stubs for AUTH-06, AUTH-08"
    - path: "apps/api/src/middleware/authenticate.test.ts"
      provides: "Test stub for AUTH-05 (401 path)"
    - path: "apps/api/src/middleware/require-role.test.ts"
      provides: "Test stub for AUTH-05 (403 path)"
    - path: "apps/api/src/db/schema/users.test.ts"
      provides: "Test stub for AUTH-04 (enum values)"
  key_links:
    - from: "apps/api/src/modules/auth/auth.test.ts"
      to: "apps/api/src/modules/auth/auth.service.js"
      via: "vi.mock — decoupled until Plan 03 implements the service"
    - from: "apps/api/src/middleware/authenticate.test.ts"
      to: "apps/api/src/middleware/authenticate.js"
      via: "vi.mock — decoupled until Plan 04 implements middleware"
---

<objective>
Install auth dependencies and create failing test stubs (RED state) for all Phase 1 requirements.

Purpose: Nyquist compliance requires test files to exist before implementation begins. Every subsequent plan's `<verify>` command references these files. Without them, executors cannot run verification. Installing packages here ensures later plans can import jose, argon2, and cookie-parser without package-not-found errors.
Output: 5 test stub files in RED state + packages installed.
</objective>

<execution_context>
@C:/Users/adity/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-rbac/01-VALIDATION.md

@apps/api/src/index.ts
@apps/api/src/db/index.ts
@apps/api/src/index.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install auth packages</name>
  <files>apps/api/package.json</files>
  <action>
    Run from repo root:
    ```
    pnpm --filter @k21/api add jose@^6.0.0 argon2@^0.44.0 cookie-parser@^1.4.7 zod@^3.23.0
    pnpm --filter @k21/api add -D @types/cookie-parser@^1.4.0
    ```

    After install, verify package.json contains these entries under dependencies:
    - jose
    - argon2
    - cookie-parser
    - zod

    And @types/cookie-parser under devDependencies.

    Do NOT pin exact versions — use the ^ range specifiers above.
  </action>
  <verify>
    <automated>cd H:/AI/k21 && pnpm --filter @k21/api test</automated>
  </verify>
  <done>All 4 runtime deps and 1 dev dep present in apps/api/package.json; existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test stubs (RED state)</name>
  <files>
    apps/api/src/modules/auth/auth.test.ts,
    apps/api/src/modules/users/users.test.ts,
    apps/api/src/middleware/authenticate.test.ts,
    apps/api/src/middleware/require-role.test.ts,
    apps/api/src/db/schema/users.test.ts
  </files>
  <behavior>
    auth.test.ts stubs (all must FAIL — implementation does not exist yet):
    - AUTH-01: login with valid email+password returns { accessToken: string }
    - AUTH-01: login with wrong password throws INVALID_CREDENTIALS
    - AUTH-01: login with deactivated user throws INVALID_CREDENTIALS
    - AUTH-02: refresh with valid token returns new accessToken
    - AUTH-02: refresh with expired/missing token throws INVALID_TOKEN
    - AUTH-03: logout deletes the refresh_token DB row

    users.test.ts stubs (all must FAIL):
    - AUTH-06: deactivateUser sets isActive=false AND deletes all refresh_tokens rows for that user (in one tx)
    - AUTH-08: createUser writes an audit_log row with action=CREATE, tableName=users, correct userId

    authenticate.test.ts stubs (all must FAIL):
    - AUTH-05: request without Bearer token → next is NOT called, res.status(401) is called
    - AUTH-05: request with valid token → req.user is set, next() is called
    - AUTH-05: mustChangePassword=true token on non-change-password path → res.status(403) with error=PASSWORD_CHANGE_REQUIRED

    require-role.test.ts stubs (all must FAIL):
    - AUTH-05: user with correct role → next() is called
    - AUTH-05: user with wrong role → res.status(403) is called
    - AUTH-05: no req.user → res.status(401) is called

    users.test.ts schema stub (all must FAIL):
    - AUTH-04: roleEnum contains exactly ['Owner', 'Finance', 'Warehouse Staff', 'Cashier', 'Admin']
  </behavior>
  <action>
    Create each file with proper vi.mock pattern (same pattern as existing index.test.ts). All imports reference the .js extension (NodeNext ESM requirement).

    Use `vi.mock` to decouple stubs from live DB and the not-yet-existing implementation files. Structure:
    - auth.test.ts: mock `../../db/index.js` and `../auth/auth.service.js`; call the service functions directly in tests
    - users.test.ts: mock `../../db/index.js`; import from `../users/users.service.js`
    - authenticate.test.ts: mock `../../db/index.js`; import from `./authenticate.js`; use express Request/Response mocks
    - require-role.test.ts: no DB mock needed; import from `./require-role.js`; use simple req/res mocks
    - users.test.ts (schema): import from `../../db/schema/users.js` (which does not exist yet → RED)

    IMPORTANT: The files being imported do not exist yet. Vitest will report import errors, which IS the expected RED state. The test runner itself must still start without crashing.

    Create the directory structure first:
    - `apps/api/src/modules/auth/` (create if not exists)
    - `apps/api/src/modules/users/` (create if not exists)
    - `apps/api/src/middleware/` (create if not exists)
    - `apps/api/src/db/schema/` (create if not exists)

    For the schema stub, place it at `apps/api/src/db/schema/users.test.ts`.
  </action>
  <verify>
    <automated>cd H:/AI/k21 && pnpm --filter @k21/api test 2>&1 | tail -20</automated>
  </verify>
  <done>
    Running `pnpm --filter @k21/api test` shows 5 test suites — existing tests pass, new stubs either fail with import errors (RED) or explicit test failures. Vitest process exits non-zero (some tests fail). No config/parse errors.
  </done>
</task>

</tasks>

<verification>
```bash
cd H:/AI/k21 && pnpm --filter @k21/api test 2>&1 | grep -E "PASS|FAIL|ERROR"
```
Expected: existing health test PASSES; new stub files show FAIL or import errors.
</verification>

<success_criteria>
- jose, argon2, cookie-parser, zod in apps/api/package.json dependencies
- @types/cookie-parser in devDependencies
- 5 test stub files created in correct directories
- All stubs in RED state (failing) — not green
- Existing GET /health test still passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-rbac/01-01-SUMMARY.md`
</output>
