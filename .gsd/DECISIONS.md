# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | arch | Marketplace integration scope for M001 | Inbound only (webhook → reserve/deduct), OAuth deferred | OAuth requires Shopee partner registration. Internal processing side can be built and tested with a partner_key for HMAC verification only. Outbound sync (stock push, escrow) needs access_token which requires OAuth. | Yes — M002 adds bidirectional | collaborative |
| D002 | M001 | data | Stock behavior on marketplace order | Reserve on order.created, deduct on order.shipped | Prevents POS overselling without immediately removing stock. Matches the existing stockReservations table pattern already used in the codebase. | No | collaborative |
| D003 | M001 | arch | Webhook processing pattern | Enqueue to BullMQ on receipt, process async | Shopee expects fast ACK (<5s). Blocking on DB + reservation inside the HTTP handler risks timeout and retry storms. Existing marketplaceQueue already defined. | No | collaborative |
| D004 | M001 | data | SKU mapping management in M001 | Seed via migration script, no UI | UI and auto-population from v2.product.get_item_list deferred to M002. Seeding is sufficient to test inbound flow. | Yes — M002 adds UI | collaborative |
| D005 | M001 | data | Schema forward-compatibility for M002 | Add sync_direction, token columns, escrow columns, shopee_item_id/model_id now | Adding columns to live tables later risks breaking migrations. Cost of adding nullable columns upfront is zero. M001 uses inbound_only + null tokens. | No | collaborative |
| D006 | M001 | arch | Webhook endpoint path | /api/v1/webhooks/shopee | Keeps it inside the versioned API prefix consistent with all other routes. Shopee callback URL must be registered — locking this now prevents having to re-register. | No | collaborative |
| D007 | M001 | data | Unresolved SKU behavior | Import order with sku_resolution_status flag, no crash, notify Owner/Admin | Orders must never be silently dropped. SKU gap is an operational issue, not a system error. Notification gives Owner/Admin immediate visibility to fix the mapping. | No | collaborative |
| D008 | M001/S01/T02 | schema | marketplace_platform enum casing for K21 marketplace tables | Lowercase values ('shopee', 'tiktok') — matching the live DB provisioned from main repo | The live DB was provisioned from a prior migration draft that used lowercase enum values. Since the DO $$ EXCEPTION block silently skips enum creation when types exist, the Drizzle schema must match the DB reality. Uppercase was the original plan spec but the DB is authoritative. | Yes | agent |
| D009 | M001/S01/T02 | schema | marketplace_order_status enum values | Live DB values (PENDING, CONFIRMED, READY_TO_SHIP, SHIPPED, DELIVERED, CANCELLED, RETURNED, STOCK_CONFLICT) over plan spec (UNPAID, RETURN_REFUND) | Same reason as D008: existing enum in live DB takes precedence. STOCK_CONFLICT added to represent unresolved-SKU state. These values must be used by S02 worker. | Yes | agent |
| D010 | M001/S01/T02 | schema | Drizzle composite unique constraint on marketplace_sku_mappings | Constraint omitted from Drizzle schema; enforced at DB level via SQL migration only | Drizzle v0.30+ pgTable 3rd-arg type inference requires object shape (PgTableExtraConfig), not IndexBuilder array. Omitting in Drizzle avoids TS error while keeping DB-level integrity. | No | agent |
| D011 | M001/S02/T01 | arch | Webhook router mounted at app level before express.json() | Mount on `app` directly (not v1Router) BEFORE express.json() with route-level express.raw() | Raw Buffer required for HMAC-SHA256 verification. express.json() consumes the body stream before express.raw() can capture it. Route-level raw middleware is the only reliable approach without disrupting global JSON parsing for all other routes. | No | agent |
| D012 | M001/S02/T01 | arch | Webhook event logging strategy — insert before HMAC check | Insert PENDING event row first; update to SKIPPED/PROCESSED/FAILED after | Every request gets a DB record regardless of validity, providing a complete audit trail. SKIPPED (invalid HMAC) is distinguishable from FAILED (worker error) in the events table. | No | agent |
| D013 | M001/S02/T02 | arch | Sentinel UUID for marketplace system actor | '00000000-0000-0000-0000-000000000001' (MARKETPLACE_SYSTEM_USER constant) | inventory_movements.performed_by is uuid type — cannot accept human-readable strings. Sentinel UUID documents the automated actor without requiring a real user row. No FK constraint exists on performed_by in Drizzle schema. | Yes — add FK + real service user in M002 | agent |
| D014 | M001/S02/T02 | arch | processOrderShipped journal entry failure is best-effort (non-fatal) | Catch journal entry failure, log, continue — decrementStock and order status update are not rolled back | decrementStock has already committed when journal entry is attempted. Re-throwing would leave the order in a SHIPPED state without the status update. A failed journal is recoverable via reconciliation; a failed stock decrement is not. | Yes — add retry/alerting in M002 | agent |
| D015 | M001/S03 | arch | Paginated list endpoint query pattern for marketplace module | Promise.all([rowsQuery, countQuery]) returning { data, total, limit, offset } | Concurrent rows + count avoids a second serialized round trip. Returning a consistent envelope shape (data, total, limit, offset) at every list endpoint lets the frontend implement generic pagination without per-endpoint special-casing. Established in S03 for getChannelOrders and getWebhookEvents; all future list endpoints should follow this shape. | Yes — switch to cursor-based pagination if sets grow large | agent |
| D016 | M001/S03 | arch | Pattern for expected service-layer failures that map to specific HTTP status codes | Service throws Error('ORDER_NOT_FOUND') (named string); router checks error.message to branch 404 vs 500 | Named error strings are lightweight, don't require a custom error class hierarchy, and are readable in logs. The router layer is the only translation point (service → HTTP). Custom Error subclasses add boilerplate with no benefit at the current scale. Pattern is consistent with how other modules signal expected failures in this codebase. | Yes — introduce typed AppError hierarchy if error catalog grows | agent |
| D017 | M001/S04/T01 | frontend | How to type backend Date columns in frontend TypeScript interfaces | Type as string, not Date, in all frontend interfaces that mirror backend Drizzle schema types | express.json() serializes Drizzle Date objects to ISO-8601 strings before they reach the browser. If the frontend interface declares Date, TypeScript allows Date methods (e.g. .toISOString()) but at runtime the value is a string — causing silent bugs. Typing as string forces explicit re-parsing via new Date(iso) at usage sites, which is the correct behavior. | No | agent |
